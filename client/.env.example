# Base API URL configuration
# In local development, the Vite proxy handles '/api' paths automatically.
# In production, set this to your deployed backend API URL (e.g., https://your-api.onrender.com/api).
VITE_API_URL=/api
